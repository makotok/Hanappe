--------------------------------------------------------------------------------
-- Hanappe Application config
--------------------------------------------------------------------------------
local config = {
    title = "Hanappe samples",
    screenWidth = 480,
    screenHeight = 320,
    viewWidth = 480,
    viewHeight = 320,
    scaleMode = "none",
    landscape = true,
    showWindow = true,
    useInputManager = true,
    mainScene = "samples/sample_scene",
}

return config