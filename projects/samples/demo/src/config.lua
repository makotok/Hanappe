--------------------------------------------------------------------------------
-- Hanappe Application config
--------------------------------------------------------------------------------
local config = {
    title = "Hanappe samples",
    screenWidth = 320,
    screenHeight = 480,
    viewWidth = 320,
    viewHeight = 480,
    scaleMode = "none",
    landscape = false,
    showWindow = true,
    useInputManager = true,
    mainScene = "samples/sample_scene",
}

return config