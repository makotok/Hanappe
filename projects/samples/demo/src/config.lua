--------------------------------------------------------------------------------
-- Hanappe Application config
--------------------------------------------------------------------------------
local config = {
    title = "Hanappe samples",
    screenWidth = 480,
    screenHeight = 320,
    viewWidth = 320,
    viewMaxScale = 2,
    landscape = true,
    mainScene = "samples/sample_scene",
}

return config